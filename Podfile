# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'FlexboxCSSTewt' do

pod 'SwiftyJSON' 

end

  target 'FlexboxCSSTewtTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlexboxCSSTewtUITests' do
    # Pods for testing
  end
